# SOURCE

Synthesized for rag-lab. Not scraped or copied from any real executed
agreement — entirely authored for this project as generic, template-style
commercial contract excerpts.

**Stresses:** Numbered clause structure (`## N. Section Name` headings with
`N.M` clause paragraphs beneath), to demonstrate parent-document retrieval —
a query touching one clause often needs the surrounding section for full
context, which is exactly what parent-document retrieval is designed to
recover.

**Scale:** 14 documents. `doc_id = sha1(corpus + relative_path)[:12]`, so this
corpus can grow later without breaking any downstream artifact.
